{
  "id": "firewall-ufw",
  "title": "Lock down your server with a firewall",
  "type": "series",
  "difficulty": "advanced",
  "tags": ["firewall", "security", "ufw", "ports"],
  "series": {
    "track_id": "deploy-like-a-real-engineer",
    "track_title": "Deploy like a real engineer",
    "order": 4,
    "requires": ["nginx-reverse-proxy"],
    "requires_description": "nginx must be proxying requests to Node app on port 3000",
    "environment": "vps"
  },
  "problem": "Your app is running and accessible but your server is wide open. Any port that has a service on it is reachable from the internet. Lock it down — only SSH and HTTP should be accessible from outside. Everything else gets dropped.",
  "setup": {
    "description": "Check what is currently exposed on your server.",
    "commands": [
      "sudo ss -tlnp",
      "sudo firewall-cmd --state"
    ],
    "note": "ss shows every port currently listening. firewall-cmd checks if firewalld is already active on your Amazon Linux instance. Amazon Linux 2023 uses firewalld not ufw — if firewalld is running use firewall-cmd, if not we will set it up."
  },
  "hints": [
    "Amazon Linux uses firewalld not ufw — the commands are firewall-cmd not ufw. Start with sudo systemctl start firewalld and sudo systemctl enable firewalld",
    "Add rules for SSH and HTTP before enabling the firewall — if you block SSH before allowing it you will lock yourself out of the server permanently",
    "firewall-cmd --permanent makes rules survive reboots, without --permanent rules are lost on restart. Always follow with firewall-cmd --reload to apply"
  ],
  "commands": ["firewall-cmd", "systemctl", "ss", "curl"],
  "solution": "1. Check listening ports:\n   sudo ss -tlnp\n   # See everything currently exposed.\n2. Install firewalld:\n   sudo yum install -y firewalld\n   # Install if not already present.\n3. Start firewalld:\n   sudo systemctl start firewalld\n   # Start the service immediately.\n4. Enable on boot:\n   sudo systemctl enable firewalld\n   # Ensures firewalld restarts after a reboot.\n5. Check current rules:\n   sudo firewall-cmd --list-all\n   # See the default zone rules before making changes.\n6. Allow SSH first:\n   sudo firewall-cmd --permanent --add-service=ssh\n   # Do this before anything else — never lock yourself out.\n7. Allow HTTP:\n   sudo firewall-cmd --permanent --add-service=http\n   # Opens port 80 for nginx.\n8. Reload rules:\n   sudo firewall-cmd --reload\n   # Applies permanent rules to the running firewall.\n9. Verify rules:\n   sudo firewall-cmd --list-all\n   # Confirm ssh and http are listed under services.\n10. Test SSH still works:\n   open a second terminal and SSH in\n   # Confirm access before closing your current session.\n11. Test app still works:\n   curl http://YOUR_EC2_IP\n   # nginx should still respond on port 80.\n12. Test port 3000 blocked:\n   curl http://YOUR_EC2_IP:3000\n   # Should timeout or refuse — Node app must not be directly reachable."
}